import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        height: 44,
        width: 200,
        backgroundColor: '#fff',
        borderRadius: 4,
        paddingHorizontal: 16,
        marginTop: 4,
        fontSize: 14
    }
})